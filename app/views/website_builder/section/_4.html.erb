<div class="row">
  <div class="col-md-3">
    <h1><%= section.columns.order(:position).first.header unless section.columns.order(:position).first.header != "" %></h1>
    <h2><%= section.columns.order(:position).first.subheader unless section.columns.order(:position).first.subheader != "" %></h2>
    <%= raw section.columns.order(:position).first.content unless section.columns.order(:position).first.content != "" %>
  </div>
  <div class="col-md-3">
    <h1><%= section.columns.order(:position).second.header unless section.columns.order(:position).second.header != "" %></h1>
    <h2><%= section.columns.order(:position).second.subheader unless section.columns.order(:position).second.subheader != "" %></h2>
    <%= raw section.columns.order(:position).second.content unless section.columns.order(:position).second.content != "" %>
  </div>
  <div class="col-md-3">
    <h1><%= section.columns.order(:position).third.header unless section.columns.order(:position).third.header != "" %></h1>
    <h2><%= section.columns.order(:position).third.subheader unless section.columns.order(:position).third.subheader != "" %></h2>
    <%= raw section.columns.order(:position).third.content unless section.columns.order(:position).third.content != "" %>
  </div>
  <div class="col-md-3">
    <h1><%= section.columns.order(:position).fourth.header unless section.columns.order(:position).fourth.header != "" %></h1>
    <h2><%= section.columns.order(:position).fourth.subheader unless section.columns.order(:position).fourth.subheader != "" %></h2>
    <%= raw section.columns.order(:position).fourth.content unless section.columns.order(:position).fourth.content != "" %>
  </div>
</div>